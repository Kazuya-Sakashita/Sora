# ISSUE-001: データ永続化がない（リロードで全データ消える）

## 基本情報

| 項目 | 内容 |
|------|------|
| 優先度 | critical |
| ステータス | open |
| 関連画面 | 全画面 |
| 関連ファイル | `lib/app-context.tsx` |

## 問題の概要

ペット情報・思い出・気持ち記録がすべてReact Stateのオンメモリ管理のため、ページリロードで消える。

## 詳細

`lib/app-context.tsx` の `useState` で管理しているすべてのデータ（pet, memories, feelings）がブラウザリロードで初期化される。ユーザーが記録を積み重ねることがこのアプリの根幹なのに、保存されないのは致命的。

## 期待する動作

- ペットプロフィールがリロード後も残っている
- 思い出記録がリロード後も残っている
- 気持ち記録がリロード後も残っている
- 初回起動時はオンボーディングを表示し、プロフィール登録済みならホームを表示

## 実装方針

`localStorage` を使ってデータを永続化する。
- `useLocalStorage` カスタムフックを作成するか、useEffect で localStorage に同期する
- キー設計: `sora_pet`, `sora_memories`, `sora_feelings`, `sora_current_screen`
- 初回判定: `sora_pet` が存在するかどうかで onboarding/home を切り替える

## 完了条件

- [ ] ペット登録後にリロードしてもホーム画面が表示される
- [ ] 思い出を追加後にリロードしてもタイムラインに残っている
- [ ] 気持ち記録がリロード後も残っている
- [ ] 既存機能に影響なし確認済み
- [ ] ステータスを done に更新した
